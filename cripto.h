long long codificar(int arm);	       //Deixar para depois
int decodificar(long long cod);       //Deixar para depois
int ligarBit(int arm, int pos);    //Recebe um conjunto de 32 bits e recebe a posição para ligar
int desligarBit(int arm, int pos); //Recebe um conjunto de 32 bits e recebe a posição para desligar
bool testaBit(int arm, int pos);
void printCod(long long cod);